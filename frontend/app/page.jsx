import Dashboard from '../components/Dashboard'; // Import the Dashboard component

export default function RootPage() {
  // Render the Dashboard component when someone visits the base URL
  return <Dashboard />;
}